Wuji training-termination pair: complete development comparison

The earlier fixed first-seed44 traces show that native survival and strict body stability differ. On wider2s development resets, the reset2x finalCP100 keeps292/300 trials natively alive,274/300 strictly body-stable and262/300 successful. Of26 strict body failures,20 recover for at least9frames; the median time actually outside the strict body envelope is0.1s. These unchanged rollouts do not establish that early termination improves learning. Existing absolute-pose costs already penalize drift. Offline audit: termination-gap-analysis-20260923-v1 (no new physics).

This pair tests only training termination thresholds: native5cm/1.57rad versus strict1cm/0.25rad. Both start from the same original frozen teacher and input statistics with a fresh Adam, seed20261204,2x online reset perturbations, adaptive input statistics, LR1e-5,5120 environments x32 horizon x100 epochs per arm. Each also has a10000-transition actual runtime gate and a fresh3-epoch PPO preflight; formal training restarts from the original. Rewards, absolute pose cost, clocks,20DoF action mapping, realistic147x19x11mm knife and physics are unchanged. No reported evaluation row is sampled for training; only the same three original training grasps are perturbed online.

Every CP10/25/50/75/100 and the original baseline is evaluated,44 total conditions, on the SAME eightH100GPU1 with batch332 and evaluation seed20261125. The evaluator uses the ORIGINAL native termination for all models and scores all trajectories with the same strict rule; the proposed training threshold cannot manufacture an evaluation improvement. Do not mix this baseline with localGPU or otherH100 evaluation batches. All traces, failures, body and endpoint metrics are retained and independently rescored.

Each condition runs20s at30Hz control/120Hz physics, externally switched2s or5s goals. All command endpoints require the final9frames within2mm, and body pose must remain under10mm/0.25rad for the whole trial, alive and finite. Both observed development cohorts contain100 states per three trained grasps plus32 unchanged fourth-grasp states, reported separately. The95% curve is a working selection target. This is not new independent, unseen-geometry, broad-grasp or hardware validation. A selected candidate still needs freezing and fresh independent states. The RGB/runtime remains bound to the original teacher; there is no silent teacher replacement.

The native implementation terminates on > while the evaluator accepts <. Equality is documented; the experiment does not rewrite the shared task comparator. The runtime gates check actual configured thresholds and termination masks on10000 training transitions; formal/preflight logs are independently audited for resolved thresholds. Sharpa reference branches retain their existing protocol. Hardware damping, friction, camera and actuator delays remain uncalibrated.

Additional predeclared training seed. Additional paired seed20261204, fixed100epochs perarm, originalteacher/newAdam/LR1e-5/5120x32,2xresetnoise. Only native versus strict training termination differs. Same physicalGPU within training pair and same evaluationGPU within pair; no fresh independent or hardware claim.
