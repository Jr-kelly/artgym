# Wuji 初态扰动范围配对续训

2026-09-23 12:26 UTC。阶段记录，最终44条件未完成；后续以全量审计为准。

## 问题与对照

冻结 RGB 在另300个两倍初始扰动状态上未通过工作门槛；直接提供真实物体状态的 teacher 也失败。慢速 teacher 的34个失败全涉及身体稳定性，只有11个身体失败发生在前0.5秒。这支持继续诊断持握与操作稳定性，不能将问题全部归因于视觉估计。

两臂从相同原teacher CP25、normalizer、新Adam、seed20261124开始；LR1e-5，5120并行环境×32步，每臂100轮，共16,384,000转移。只改变在线重置扰动：1x为±0.5mm位置/±0.01rad关节/±0.5度旋转向量分量；2x为其两倍。训练只用三个原始抓姿，不采样评估行。每臂先10000实际转移范围检查，再3轮PPO预检，正式训练重新初始化。聚合KL预检不等于同策略精确KL。

训练、下载、评估使用固定源码和独立新目录。44个开发评估条件固定为：原teacher基线及两臂CP10/25/50/75/100，分别小扰动/已观察两倍扰动，2秒/5秒外部命令。每个条件全332初态，前三组各100，第四旧未训练抓姿32一并保留。20秒实际闭环，2mm端点最后9帧保持、刀身全程10mm/.25rad、存活且有限。评估不按到位触发换命令。

## 当前证据

本机 batch332 的基线：小扰动298/299，大扰动252/263（快/慢，每项分母300）。不能与先前4×83或H100数值直接混用。

两倍扰动CP25：小扰动296/300，大扰动268/278。宽扰动快速修复27个原失败、损失11个原成功，净增16；慢速修复22、损失7，净增15。开发配对bootstrap95区间快速约1.3–9.3个百分点、慢速1.7–8.3个百分点；单seed、多个CP、未校正多重比较，只作描述。1x控制未完成评估，不能把改善唯一归因于扩增范围。

宽扰动CP25的身体稳定数快/慢275/281，仍有7/3个身体稳定但端点不合格。前三组快速成功84/93/91、慢速86/95/97，第一抓姿仍最弱。所有第四抓姿仍0/32。没有任何一个新的宽扰动候选在这里达到95%总体和每组90%门槛。

离线控制分析：`diagnostics/reset-range-control-analysis-20260923-v1/analysis.json`，13/44条件，经已审计trace的SHA再次验证。支撑目标限幅与目标跟踪误差按第一次身体失败之前的有效转移统计，避免使用掉落后的动作解释先前失败。第一/第二抓姿常用到支撑限幅，但第三抓姿原teacher的循环失稳也发生在远离限幅的轨迹；没有证据将所有失败都归因于支撑±.04rad。成功与失败的跟踪误差大量重叠，不能直接以其大小认定执行器或限幅为原因。

## 下一步与停止重复的规则

1. 收齐两臂全部44条件、正常退出和逐条重评分，报告完整CP曲线、最终CP100和配对净变化。
2. 使用已有q、目标、动作和物体稳定性轨迹定位失败阶段；没有控制限幅的因果证据前，不盲目扩大范围或改刚度。
3. 若一个teacher候选同时保留小扰动并改善宽扰动，先冻结候选，再验证与视觉估计器的兼容性。当前RGB/runtime明确绑定原teacher SHA，不能伪造元数据强行替换。
4. 所有当前小/宽开发数据均已观察；调参后必须生成新独立初态验收。新抓姿、未见几何、真实标定和真机均是另外的验证层次。

原Sharpa成功依赖功能抓姿/物性有效标定、当前真值teacher及自身轨迹蒸馏；官方10mm到位循环协议和人工预放置，与本任务固定时钟2mm以及最终真机目标不同。完整既有研究与失败见 `sharpa-success-and-wuji-student-gap-20260922.md`，不重复已失败的纯本体感觉MSE/盲目延长训练尝试。


## 13:02 UTC：完整预算、退化位置与后续诊断

两臂100轮均正常结束，正式共32,768,000转移。`reset-range-training-final-audit-20260923-v2.json`核验完整100个epoch/frame/LR点、10个有限权重、运行门禁报告和正常退出；v1只据配置列门禁预算，v2补查实际10000转移报告，两版均保留。

两倍范围CP100小扰动294/298、宽扰动262/279；宽快速分组92/75/95，不能只报早期峰值。CP50宽快速刀身稳定275与CP25一致，但联合268降到248；稳定刀身中的关闭后滑块偏离增加，第三抓姿最后关闭阶段的误差尾部约4mm。原范围CP10宽253/274、CP25宽253/274；完整后续对照尚未齐。

实际输入normalizer在两臂都继续累计：原count73728001，CP100为93388801。两倍范围CP100的最大均值变化约0.208个原标准差，标准差比值范围约0.889–1.248；这是描述，不是唯一失败原因。冻结诊断v3配对原/继续权重与原/继续输入统计，并加入恒定初始目标持握，统一在H100GPU2建立新数值基线。旧GPU1等待v1在零计算取消；GPU2 v2因固定评估器无cleanup hook未保存intervention元数据而退出1，native/noop的全部10个trace数组已逐元素证实一致。v3只改结果核验/保存生命周期，正式16条件之前重跑所有门禁。

为确认第一seed的差别是否稳定，第二seed20261128已预先固定相同学习率、原teacher/normalizer、新Adam、5120×32×100/臂和评估CP10/25/50/75/100；顺序1x后2x，使用新运行目录，未修改奖励或控制。它等待既有GPU1 CP评估释放租约，原44项评估继续，第二套44项在其后进行。种子复验不是新物体/独立初态验证。

下一步先读冻结诊断及两个seed的完整CP结果。若归一化交换能恢复关闭精度，须另做冻结/更新统计的训练对照，不能把混合统计的反事实直接命名为原训练成果。若持握或关闭偏离仍在，考虑训练目标与严格外部时钟末端保持的一致性：现有训练是到位一次奖励加逐帧近目标奖励，而验收要求每一阶段最后9帧均合格及刀身全程稳定。可提出保留原奖励、增添阶段末保持奖励的零/非零配对；目前仅是假设，尚未实现或开训，先确认现有机制结果，避免重复无依据参数扫描。
