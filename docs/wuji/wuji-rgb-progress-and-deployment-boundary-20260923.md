# Wuji RGB 进展及部署边界

记录时间：2026-09-23T10:27:57.441835+00:00。以下结果全部注明评估范围；新独立队列尚未完成。

## 当前证据说明什么

- 冻结 RGB 状态估计器加冻结 teacher actor，在同一旧开发三抓姿×100扰动上，原图像数据模型快268/300、慢294/300；加入自身闭环状态图像、相同最终5000更新预算后为290/300、297/300。两者均包含第四抓姿32次失败对照，第四全部0。每个版本16条件/796800转移均从原轨迹重算。
- 屏蔽图像的匹配对照原为105/135，混合数据后135/202。视觉信息有实际闭环收益；不能把两个纯本体学生结构之间的差异都解释成视觉因果效应。
- 同一旧验证图像/关节/初态，单独翻转开合命令会使原RGB/混合RGB滑块预测改变RMS约0.214/0.143mm；屏蔽图像约1.264/1.231mm。这是命令相关的状态估计偏差诊断，不是单独证明所有失败的原因。只用了2400旧验证例，不能替代全量拟合验证。
- 纯关节增量DAgger追加1500（共2000）最终217/262。拇指监督损失按动作尺度加权12倍，同预算500更新147/234，差于未加权203/269。这些对照不支持“只需要继续相同训练或改损失单位就一定成功”。

## 与原论文 Sharpa 的关系

论文使用privileged teacher、功能抓姿、人工放置、针对物体的有效物性标定，并按部署目的筛选抓姿；student性能差距在附录A.2有讨论。当前参考复核CP3400/1.088B帧：全部287抓姿×100次，上游54.93%、奖励修正53.92%，差值的分层配对区间跨0；前50次选每物体top5、后50次报告时为99.20%和99.92%。筛选口径和所有候选平均值不能互换，10mm循环指标也不同于本任务每段末0.3s均<2mm的固定时钟指标。

当前RGB路径是明确的Wuji迁移实验：teacher仍来自RL，学生学习可解释的物体状态，再交给冻结actor；它增加了视觉传感器，**不是论文原本纯本体TCN latent-MSE学生的原样复现**。不能以相机路线成功宣称纯关节学生已经达到相同可靠性。

## 独立验证的冻结规则

两个最终模型先冻结，再生成seed20261120的三抓姿各100个新无筛选扰动；旧第四32只作失败控制。两候选、两时钟、四个83状态批次全报。原本机队列保持不变，H100容量预检76/83而本机原77/83，提示数值条件不同，未据此更换主评估硬件。模型不得在看到这批结果后再修改而继续称其独立；下一轮改模型必须生成新队列。

预注册工作标准：每时钟总体至少95%、各训练抓姿至少90%，附Wilson区间和配对结果。该范围只涉及已训练抓姿附近的小扰动、同一刀具和相机，不能外推到未见抓姿/几何或实物。

## 推理接口与真机之间还差什么

新增接口只需当前RGB、编码器关节、外部开合命令和reset时已知几何，通过URDF FK得到指尖位置，不创建物理环境，不访问当前物体真值。旁路5400转移的输入/动作/目标校验已完成。让接口真正驱动仿真的校验仍在进行；此前失败显示必须保留完整目标归一化/还原和平滑的浮点控制链路，不能只凭代数等价就认为累计拇指目标逐步一致。

实际手仍需确认图像采集能力、相机内外参和图像域差异；初始刀具位姿需要人工放置夹具或测量流程。Wuji输出为位置目标，当前有效增益/阻尼与接触物性尚未进行实物标定。论文用独立标定物体开环重放成功teacher动作筛选物性设置；目前仿真147×19×11mm、35g、阻尼0.3不能代替这一步。没有向硬件发送命令，也没有真实抓取或使用刀具的结果。

证据：`diagnostics/rgb-mixed-final-rescored-1802-v1.json`、`rgb-policy-final-rescored-1652-v2.json`、`rgb-goal-counterfactual-1738-v1/result.json`、`command-continue-final-rescored-1727.json`、`command-thumbunits-final-rescored-1654.json`、`sharpa-matched3400-final100-rescored-1639-v2.json`、`rgb-sensor-shadow-all-rescored-1820-v1.json`。
