# Wuji input-normalizer continuation: complete paired development experiment

Both arms start from the same original frozen teacher and input statistics, a fresh Adam optimizer, seed20261129, LR1e-5 and the same 2x reset perturbations. Only input-normalizer updating differs: frozen versus adaptive. Actor, critic and value statistics remain trainable. Each arm runs5120 environments x32 horizon x100 epochs=16,384,000 formal transitions, plus a3-epoch fresh PPO preflight and10,000-transition reset gate. The original three training grasp rows are used with online perturbations; evaluation rows are never training samples. Rewards, dynamics, realistic knife geometry and action mapping are identical. Every declared CP10/25/50/75/100 is retained, including regressions.

All44 evaluations use one eight-H100 host GPU0, batch332, seed20261125, and freshly measured frozen-baseline conditions. Never combine these counts with localGPU batch332 or the mechanism diagnostic at seed20261127. The two previously observed development cohorts contain100 perturbations of each of three trained grasp families plus32 unchanged fourth-grasp rows. Both arms are tested on both cohorts with2s/5s external commands.

Each trial lasts20s,30Hz control/120Hz physics. Every stage must end with9 consecutive frames within2mm of its goal, and the object base must stay below10mm/.25rad drift for the entire valid/alive trial. The fourth grasp remains in all raw traces and is reported separately. These are observed development rows, not an independent test, novel-grasp/object result or hardware validation. Any candidate requires subsequent freezing and fresh independent evaluation; the original RGB runtime has not been silently switched to a continued teacher.

The all-CP plots include both task success and body stability. Paired repairs/regressions and grasp-stratified bootstrap intervals are descriptive and unadjusted. Training audits verify finite weights, actual budgets, logs, unchanged input statistics at every frozen-arm checkpoint and continued updating in the adaptive arm. This trained experiment is distinct from the earlier frozen-weight counterfactual statistic swaps. The complete source, fixed policies, initial states, raw trajectories and training gates/logs are in the archive. Physical damping/friction and execution timing still require real hardware calibration.
