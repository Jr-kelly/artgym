# 当前 Goal：Wuji teacher + student 美工刀开合

用户最新澄清（2026-09-24）具有最高优先级：仿照 ArtGym，把 Sharpa 换为 Wuji，从已经持刀的功能抓姿开始，完成 teacher → student 与美工刀伸缩。桌面取刀后续考虑。

本次交付是 teacher 和 student 两个学习策略、可复现配置/权重、独立闭环评估及无字视频。刀具保持现实尺寸（当前 147×19×11 mm）。Student 以 ArtGym 的历史本体感觉蒸馏路线为主；任何视觉输入、结构、控制、物性或奖励差异单独记录。已有 RGB 状态估计路线作为另列成果，不能冒称与原 student 完全相同。

真机标定、桌面抓取、未见刀具和更广泛抓姿泛化不再是本 Goal 的完成前置条件。此前文档含这些前置条件的“总体目标未完成”描述属于旧范围；历史结果、失败和边界仍有效。不能因为范围收敛就直接把旧 student 失败改成成功。

当前重点：复用已能开合的冻结 teacher，优先查验和训练 student。停止扩大 teacher 消融矩阵。已经开始的有界多 seed 训练/评估/打包正常收尾；Sharpa 参考作为方法证据，不再阻塞 Wuji student。

评估分开记录：既有 2/5 秒固定时钟、2 mm 末段保持属于严格工程诊断；补做论文启发的 10 mm 到位换向闭环实验，必须真实重新运行物理。先从旧轨迹重算到位/开合分解，只能称离线重评分，不能当作到位换向新实验。完成判定依据冻结策略的新初态物理测试、实测行程/循环/掉落与视频；不得依靠脚本重放、teacher 接管或筛掉失败行。

执行要求仍保留：新实验使用独立固定源码和新运行目录；保留本机原训练 PID 88339；每五分钟检查资源/CP；整机 GPU 四小时平台门槛 26%，本地目标 >40%，仅运行有用工作。维护此文档、handoff、goal-state、events 并远端哈希回读。

Goal 工具当前仍返回 blocked，且本会话没有 resume/修改 objective 的 callable 接口。用户已明确授权继续，工程工作继续；不虚报工具状态，也不把此界面限制当作训练阻塞。
