# ArtManip / Wuji 论文对齐审计（2026-09-21）

结论：目前没有一套已执行并验证的实验与论文完全对齐。两个多抓姿训练属于 Wuji 移植；单抓姿低阻尼实验是物理/控制对照。持续 0 完整循环成功、回合缩短而奖励上涨，不能作为正常收敛或成功复现的证据。

论文：<https://arxiv.org/html/2609.12498v1>。核对基于 §3.3、§4.1、附录 C.1–C.6 和表 6–9。主仓库固定在 `63b94fb3364596db51b7e4651b3c3c98ff994710`，目前 GitHub main 仍为此提交。

## 论文与实际运行的差异

| 项目 | 论文 | 当前多抓姿 Wuji 实验 | 判断 |
| --- | --- | --- | --- |
| 手 | 22 自由度 Sharpa 左手 | 20 自由度 ArtBot Wuji 右手 | 必要移植差异，不能声称原实验完全相同 |
| 动作增量 α | §3.3 明确 1/40 = 0.025 rad/步 | `dofSpeedScale * dt = 1 * 0.0083333`，约 1/120 | 当前是论文的约 1/3，需修正或明确作为消融 |
| 仿真/控制频率 | 120 / 30 Hz | 120 / 30 Hz | 一致 |
| 回合上限 | 1200 控制步，40 s | 1200 控制步，另有单阶段 10 s 超时和停留判定 | 不可只根据回合长度认定终止协议一致 |
| 稳定性奖励课程 | 第 100 轮开始，历时 1000 轮；第 1100 轮到最终权重 | 本机 200→2000；四卡 50→500 | 不一致，四卡缩放只对齐了本机采样进度 |
| 并行环境 | 20000 | 本机 2560；四卡合计 10240 | 不一致 |
| Mini-batch / 探索组 | 表 9 为 40000 / 4000 | 每 rank 8192 / 512 | 不一致；论文未完整说明这些数值在双卡进程间的映射 |
| 算法/网络 | SAPG、LSTM 512、MLP 512/256/128、latent 16 | 相同主要结构，观测维度随手改变 | 方法复用 |
| LR / γ / λ | 2e-4 / .998 / .95 | 相同 | 一致 |
| 刀具几何 | 表 6 类别尺寸分布 | NT A-300GR 尺寸附近的窄分布，35 g | 用户目标所需差异，不是表 6 复现 |
| 质量随机化 | 刀柄 40–200 g，滑块 10–20 g | 刀柄 29 g、滑块 6 g，各 ±30% | 不一致 |
| 摩擦/阻尼 | 2–4 / 700–1300，Sharpa 有效标定参数 | 沿用相同范围，尚无 Wuji 实物标定 | 数值一致，但迁移适用性未验证 |
| 抓姿生成 | 官方功能接触模板，1000 候选/物体，1 s 持握筛选 | 自行实现模板；额外姿态/可达性/2 s 严格接触筛选，811 train | 数据分布不同 |
| 训练/评估容差 | 5 mm / 10 mm | 5 mm / 5 mm | 当前评估比论文严格 |
| 评估重复数 | 每个抓姿 100 次随机仿真 | 每个抓姿 5 次 | 当前为巡检协议，不是论文基准 |
| 评估对象 | 5 个未见测试几何，报告 IC/GC/SR/CSC | 周期评估仅 000/010/020 的 test 抓姿；held-out 基准尚未完成 | 当前巡检不能代替论文测试 |

按 20000 总环境 × horizon 16 理解论文，全局 rollout 为 320000 transitions，课程起点/终点约为 32M/352M transitions。本机和四卡现配置约为 8.192M/81.92M。相同最终奖励权重、相同约 2B 步预算，不代表相同课程或优化过程。

单抓姿对照还另外改变了：绝对动作、120 Hz 控制、阻尼 .3、无物性随机化、一个抓姿、640 环境、初始探索方差、无随机停留、12 s 回合。这些条件使它可用于验证已知轨迹在 RL 环境中的可行性，不能用来声称论文复现或类别泛化。

## 上游代码相同，也存在需要处理的逻辑问题

AST 比较确认，本地 `compute_reward`、`pre_physics_step`、`_get_dones`、`update_goal` 及奖励课程方法与固定上游提交相同。但公开论文与上游默认配置本身存在上表中的差异。

实际执行奖励和目标切换方法的最小复现发现：

1. 旧目标 40 mm，当前滑块 39 mm，上一最佳误差 2 mm，因此本步推进应为 1 mm，加权奖励为 +1。
2. `compute_reward` 在计算推进项前调用 `update_goal`，把目标改为 0，并将最佳距离设为 39 mm。
3. 后续计算仍使用旧目标误差 1 mm，得到 `(39−1) mm * 1000 = +38`，随后又把新阶段最佳误差覆盖为 1 mm。
4. 因此开合切换时会出现错误的推进奖励，且收回阶段的正确距离改善被历史最佳值抑制。
5. 同一测试中，若环境已标记掉落但滑块达到目标，代码仍可同时给成功 +50 和掉落 −1。该现象在代码层面已复现；是否构成实际学习中的主要投机路径仍需真实 rollout 诊断。

复现执行的是实际函数，未修改生产训练代码。文件：`tmp/paper-review/reward-audit/reproduce.py`、`report.json`。该测试证明记账/同时成功掉落的行为，不能单独确定所有训练失败的因果。

平滑项也存在论文/代码语义差异：表 7 写的是归一化动作差平方，代码罚关节位置目标差平方。成功项表述为距离指示量，代码采用停留门槛与一次性奖励。严格复现需要明确以哪一份协议为依据，不能将两者混写。

## 官方生成源码已重新取得

本次 GitHub API 和完整克隆均成功，且提交与主仓库 gitlink 完全一致：

- `youngcv/make_data`: `eafa7b6e84811de211757119081f86b3f236495f`。
- `youngcv/func_lygra`: `55f60c7e8f8db2ab55b50f6af5a07efcad191f57`。

代码保存在隔离审计目录 `tmp/paper-review/upstream-make-data` 和 `upstream-func-lygra`。先前“无法获取官方子仓库”的历史结论已失效；本次不推断当时访问失败的具体原因。

已确认的实现差异：官方 knife 标签只允许四指接触刀柄 **−Y 面**，拇指接触滑块 **+Y 面**；我们的生成器允许更广的刀柄表面并排除滑块行程区域，两者不等价。官方尺寸采样为分层分箱后各轴打乱组合；本地为独立均匀采样。滑块初始位置/关节下限也采用不同构造。

官方 `func_lygra` 已有 Wuji 接口与接触规则，但使用 `finger1_*` / `palm_link` 命名，我们的 ArtBot 资产使用 `hand_r_*`，不能直接当成同一资产和坐标系。需要核对关节顺序、坐标、碰撞面和接触法向后复用。论文的已报告手型仍为 Sharpa，仓库包含 Wuji 接口不等于论文验证过 Wuji。

## 修正后的实验顺序与验收

以下是需要落实的修正协议，不是已经跑通的严格复现：

1. 固定论文、主仓库和两个子仓库版本，使用官方生成/标签代码；先用原 Sharpa 配置做原实现参考，验证生成→持握→操控链路。
2. 建立显式论文协议配置：α=.025，120/30 Hz，课程 100→1100，训练 5 mm/评估 10 mm，并记录环境数、全局 batch、优化步数和探索组映射。官方默认与论文有冲突时，在配置与报告中列出选择，不悄悄称两者相同。
3. 对奖励目标切换、掉落与成功同时触发、开/合各自的推进奖励做回归检查。先验证保持、成功开合、掉落和无效状态的奖励及终止排序，再进行正式训练。必要修复应有单独对照，不能算作未经修改的上游复现。
4. Wuji 迁移只保留已声明的机器人映射和用户要求的刀具尺寸差异；官方接触模板映射到 ArtBot 坐标后，重验并重建受影响抓姿。先达到单案例完整成功，再扩大至多抓姿和多几何。
5. 定期巡检继续使用轻量协议。最终泛化报告用未见几何和每抓姿 100 次随机 rollout；完整开合成功、IC/GC/SR/CSC 分开报告。成功 teacher 后才进入 student 蒸馏与真机标定。

现有训练与每 5 分钟巡检继续保留。此次审计没有重启长训练、修改生产奖励、重标原数据或宣布已完成论文复现。

机器可读审计：`tmp/paper-review/alignment-audit-20260921.json`；论文摘录：`tmp/paper-review/paper-excerpts-20260921.json`。
